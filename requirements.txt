# Versiones elegidas para tener wheels precompiladas cp313 + manylinux aarch64
# (necesario tras pasar a python3.13 + arm64; evita compilar desde fuente en el build).
pymongo[srv]==4.10.1
pydantic==2.10.6
aws-lambda-powertools==2.43.1
python-dotenv==1.0.1
Pillow==11.1.0
sentry-sdk==2.18.0

# Para habilitar Lambda SnapStart (activa el hook afterRestore de database.py), descomenta:
# snapshot-restore-py==1.0.0
