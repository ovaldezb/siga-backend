service: siga-backend
useDotenv: true

provider:
  name: aws
  runtime: python3.11
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  memorySize: 256
  timeout: 30
  versionFunctions: false
  iam:
    role:
      statements:
        - Effect: "Allow"
          Action:
            - "cognito-idp:AdminCreateUser"
            - "cognito-idp:AdminDeleteUser"
            - "cognito-idp:AdminDisableUser"
            - "cognito-idp:AdminEnableUser"
            - "cognito-idp:AdminGetUser"
            - "cognito-idp:ListUsers"
            - "cognito-idp:AdminUpdateUserAttributes"
            - "cognito-idp:AdminAddUserToGroup"
            - "cognito-idp:AdminRemoveUserFromGroup"
            - "cognito-idp:AdminListGroupsForUser"
            - "cognito-idp:ListUsersInGroup"
          Resource: 
            - !GetAtt CognitoUserPool.Arn
        - Effect: "Allow"
          Action:
            - "s3:PutObject"
            - "s3:GetObject"
            - "s3:DeleteObject"
            - "s3:PutObjectAcl"
          Resource: 
            - !Sub "arn:aws:s3:::${self:service}-${self:provider.stage}-media/*"
        - Effect: "Allow"
          Action:
            - "s3:PutObject"
            - "s3:GetObject"
          Resource: 
            - !Sub "arn:aws:s3:::${self:service}-${self:provider.stage}-evidencia/*"
  environment:
    STAGE: ${self:provider.stage}
    MONGO_USER: ${env:MONGO_USER, ''}
    MONGO_PASSWORD: ${env:MONGO_PASSWORD, ''}
    MONGO_HOST: ${env:MONGO_HOST, ''}
    MONGO_DB: ${env:MONGO_DB, 'siga'}
    COGNITO_USER_POOL_ID: !Ref CognitoUserPool
    COGNITO_CLIENT_ID: !Ref CognitoUserPoolClient
    S3_MEDIA_BUCKET: ${self:service}-${self:provider.stage}-media
    S3_EVIDENCIA_BUCKET: ${self:service}-${self:provider.stage}-evidencia
    # Default mock para dev/staging. El GH secret CLIENT_LINK_SECRET sobreescribe este
    # valor en el pipeline (deploy-dev.yml lo appendea al .env antes de `sls deploy`).
    # ANTES DE PRODUCCIÓN: setear CLIENT_LINK_SECRET en repo secrets y rotar este default.
    CLIENT_LINK_SECRET: ${env:CLIENT_LINK_SECRET, 'mock-dev-secret-NOT-FOR-PROD-rotate-via-gh-secret-2026'}
    CLIENT_LINK_BASE_URL: ${env:CLIENT_LINK_BASE_URL, ''}
    # Sentry — DSN vacío deja el SDK deshabilitado (sin tráfico ni costo).
    # Setear SENTRY_DSN en GH Actions secrets para activar reporte de errores 5xx.
    SENTRY_DSN: ${env:SENTRY_DSN, ''}
    SENTRY_ENVIRONMENT: ${env:SENTRY_ENVIRONMENT, ''}
    SENTRY_TRACES_SAMPLE_RATE: ${env:SENTRY_TRACES_SAMPLE_RATE, '0'}
    CLIP_API_KEY: ${env:CLIP_API_KEY, ''}
    CLIP_SECRET_KEY: ${env:CLIP_SECRET_KEY, ''}

plugins:
  - serverless-python-requirements
  - serverless-plugin-split-stacks

custom:
  pythonRequirements:
    dockerizePip: non-linux
    layer:
      name: ${self:service}-${self:provider.stage}-lib
      description: Python requirements layer
      compatibleRuntimes:
        - python3.11
  splitStacks:
    nestedStackTrace: true
    perFunction: false
    perType: false
    perGroupFunction: true
    nestedStackCount: 20

package:
  individually: true
  patterns:
    - '!node_modules/**'
    - '!tests/**'
    - '!.venv/**'
    - '!package.json'
    - '!package-lock.json'
    - '!requirements.txt'

functions:
  health:
    handler: src/handlers/common/health.handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: health
          method: get
          cors: true

  users_list:
    handler: src/handlers/users/user_manager.list_users_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: usuarios
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  users_create:
    handler: src/handlers/users/user_manager.create_user_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: usuarios
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  users_update:
    handler: src/handlers/users/user_manager.update_user_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: usuarios/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  users_me:
    handler: src/handlers/users/user_manager.get_me_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: usuarios/me
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  talleres_list:
    handler: src/handlers/admin/talleres_manager.list_talleres_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: talleres
          method: get
          cors: true

  talleres_create:
    handler: src/handlers/admin/talleres_manager.create_taller_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: talleres
          method: post
          cors: true

  pagos_suscripcion:
    handler: src/handlers/admin/pagos_manager.procesar_pago_suscripcion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: pagos/suscripcion
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  pagos_historial:
    handler: src/handlers/admin/pagos_manager.obtener_historial_pagos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: pagos/suscripcion
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  clientes_list:
    handler: src/handlers/clientes/clientes_manager.list_clientes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: clientes
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  clientes_create:
    handler: src/handlers/clientes/clientes_manager.create_cliente_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: clientes
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  clientes_get:
    handler: src/handlers/clientes/clientes_manager.get_cliente_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: clientes/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  clientes_add_vehiculo:
    handler: src/handlers/clientes/clientes_manager.add_vehiculo_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: clientes/{id}/vehiculos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  clientes_update:
    handler: src/handlers/clientes/clientes_manager.update_cliente_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: clientes/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  clientes_delete:
    handler: src/handlers/clientes/clientes_manager.delete_cliente_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: clientes/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  flotillas_list:
    handler: src/handlers/flotillas/flotillas_manager.list_flotillas_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: flotillas
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  flotillas_get:
    handler: src/handlers/flotillas/flotillas_manager.get_flotilla_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: flotillas/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  flotillas_create:
    handler: src/handlers/flotillas/flotillas_manager.create_flotilla_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: flotillas
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  flotillas_update:
    handler: src/handlers/flotillas/flotillas_manager.update_flotilla_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: flotillas/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  flotillas_delete:
    handler: src/handlers/flotillas/flotillas_manager.delete_flotilla_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: flotillas/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  items_list:
    handler: src/handlers/inventario/items_manager.list_items_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: items
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  items_create:
    handler: src/handlers/inventario/items_manager.create_item_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: items
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  items_update_stock:
    handler: src/handlers/inventario/items_manager.update_stock_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: items/{id}/stock
          method: patch
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  items_get:
    handler: src/handlers/inventario/items_manager.get_item_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: items/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  items_update:
    handler: src/handlers/inventario/items_manager.update_item_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: items/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  items_delete:
    handler: src/handlers/inventario/items_manager.delete_item_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: items/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  get_next_folio:
    handler: src/handlers/admin/folios_manager.get_next_folio_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: folios/{tipo}/next
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  list_modulos:
    handler: src/handlers/admin/modulos_manager.list_modulos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: modulos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  reportes_kpis:
    handler: src/handlers/admin/reportes_manager.get_kpis_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: reportes/kpis
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  reportes_cliente_historial:
    handler: src/handlers/admin/reportes_manager.get_customer_history_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: reportes/cliente/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ventas_create:
    handler: src/handlers/ventas/ventas_manager.create_venta_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ventas
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  taller_my_modulos:
    handler: src/handlers/admin/talleres_manager.get_my_modulos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: talleres/me/modulos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  taller_update:
    handler: src/handlers/admin/talleres_manager.update_taller_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: talleres/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  talleres_upload_logo:
    handler: src/handlers/admin/talleres_manager.upload_logo_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: talleres/{id}/logo
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  list_marcas:
    handler: src/handlers/catalogos/autos_manager.list_marcas_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: catalogos/autos/marcas
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  list_marcas_admin:
    handler: src/handlers/catalogos/autos_manager.list_marcas_admin_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: catalogos/autos/marcas/admin
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  create_marca:
    handler: src/handlers/catalogos/autos_manager.create_marca_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: catalogos/autos/marcas
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  update_marca:
    handler: src/handlers/catalogos/autos_manager.update_marca_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: catalogos/autos/marcas/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  list_modelos:
    handler: src/handlers/catalogos/autos_manager.list_modelos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: catalogos/autos/modelos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_create:
    handler: src/handlers/ordenes/ordenes_manager.create_orden_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_list:
    handler: src/handlers/ordenes/ordenes_manager.list_ordenes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_update:
    handler: src/handlers/ordenes/ordenes_manager.update_orden_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_sugerencias_pendientes:
    handler: src/handlers/ordenes/ordenes_manager.list_sugerencias_pendientes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/sugerencias-pendientes
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_get:
    handler: src/handlers/ordenes/ordenes_manager.get_orden_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_list_events:
    handler: src/handlers/ordenes/ordenes_manager.list_orden_events_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/events
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_get_upload_url:
    handler: src/handlers/ordenes/evidencia_manager.get_upload_url_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/evidencia/upload-url
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_add_evidencia:
    handler: src/handlers/ordenes/evidencia_manager.add_evidencia_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/evidencia
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ordenes_list_evidencia:
    handler: src/handlers/ordenes/evidencia_manager.list_evidencia_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/evidencia
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  # --- Enlace público al cliente para aprobar/rechazar items de cotización ---
  cliente_link_create:
    handler: src/handlers/ordenes/cliente_link_manager.create_cliente_link_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/cliente-link
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cliente_link_get:
    handler: src/handlers/ordenes/cliente_link_manager.get_cliente_link_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/cliente-link
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cliente_link_revoke:
    handler: src/handlers/ordenes/cliente_link_manager.revoke_cliente_link_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ordenes/{id}/cliente-link
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  public_cotizacion_challenge:
    handler: src/handlers/ordenes/cliente_link_manager.public_get_challenge_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: public/cotizacion/challenge
          method: get
          cors: true

  public_cotizacion_verify:
    handler: src/handlers/ordenes/cliente_link_manager.public_verify_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: public/cotizacion/verify
          method: post
          cors: true

  public_cotizacion_get:
    handler: src/handlers/ordenes/cliente_link_manager.public_get_cotizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: public/cotizacion
          method: get
          cors: true

  public_cotizacion_decidir:
    handler: src/handlers/ordenes/cliente_link_manager.public_decidir_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: public/cotizacion/decidir
          method: post
          cors: true

  list_vehiculos:
    handler: src/handlers/vehiculos/vehiculos_manager.list_vehiculos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: vehiculos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  get_vehiculo:
    handler: src/handlers/vehiculos/vehiculos_manager.get_vehiculo_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: vehiculos/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  vehiculos_create:
    handler: src/handlers/vehiculos/vehiculos_manager.create_vehiculo_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: vehiculos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  vehiculos_update:
    handler: src/handlers/vehiculos/vehiculos_manager.update_vehiculo_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: vehiculos/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  vehiculos_delete:
    handler: src/handlers/vehiculos/vehiculos_manager.delete_vehiculo_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: vehiculos/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  citas_list:
    handler: src/handlers/citas/citas_manager.list_citas_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: citas
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  citas_create:
    handler: src/handlers/citas/citas_manager.create_cita_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: citas
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  citas_get:
    handler: src/handlers/citas/citas_manager.get_cita_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: citas/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  citas_update:
    handler: src/handlers/citas/citas_manager.update_cita_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: citas/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  citas_delete:
    handler: src/handlers/citas/citas_manager.delete_cita_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: citas/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  # ─── Cotizaciones independientes (no atadas a una OS) ───
  cotizaciones_list:
    handler: src/handlers/cotizaciones/cotizaciones_manager.list_cotizaciones_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: cotizaciones
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cotizaciones_create:
    handler: src/handlers/cotizaciones/cotizaciones_manager.create_cotizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: cotizaciones
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cotizaciones_get:
    handler: src/handlers/cotizaciones/cotizaciones_manager.get_cotizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: cotizaciones/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cotizaciones_update:
    handler: src/handlers/cotizaciones/cotizaciones_manager.update_cotizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: cotizaciones/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cotizaciones_delete:
    handler: src/handlers/cotizaciones/cotizaciones_manager.delete_cotizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: cotizaciones/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  cotizaciones_convertir_a_os:
    handler: src/handlers/cotizaciones/cotizaciones_manager.convertir_a_os_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: cotizaciones/{id}/convertir-a-os
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  proveedores_list:
    handler: src/handlers/proveedores/proveedores_manager.list_proveedores_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: proveedores
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  proveedores_create:
    handler: src/handlers/proveedores/proveedores_manager.create_proveedor_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: proveedores
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  proveedores_get:
    handler: src/handlers/proveedores/proveedores_manager.get_proveedor_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: proveedores/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  proveedores_update:
    handler: src/handlers/proveedores/proveedores_manager.update_proveedor_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: proveedores/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  proveedores_delete:
    handler: src/handlers/proveedores/proveedores_manager.delete_proveedor_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: proveedores/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  sucursales_list:
    handler: src/handlers/sucursales/sucursales_manager.list_sucursales_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: sucursales
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  sucursales_create:
    handler: src/handlers/sucursales/sucursales_manager.create_sucursal_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: sucursales
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  sucursales_update:
    handler: src/handlers/sucursales/sucursales_manager.update_sucursal_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: sucursales/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  sucursales_delete:
    handler: src/handlers/sucursales/sucursales_manager.delete_sucursal_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: sucursales/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  autorizaciones_create:
    handler: src/handlers/ventas/autorizaciones_manager.create_autorizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: autorizaciones
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  autorizaciones_list:
    handler: src/handlers/ventas/autorizaciones_manager.list_autorizaciones_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: autorizaciones
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  autorizaciones_update:
    handler: src/handlers/ventas/autorizaciones_manager.update_autorizacion_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: autorizaciones/{id}
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  traspasos_create:
    handler: src/handlers/inventario/traspasos_manager.create_traspaso_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: inventario/traspasos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  traspasos_list:
    handler: src/handlers/inventario/traspasos_manager.list_traspasos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: inventario/traspasos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  traspasos_receive:
    handler: src/handlers/inventario/traspasos_manager.receive_traspaso_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: inventario/traspasos/{id}/recepcion
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  caja_get_activa:
    handler: src/handlers/caja/caja_manager.get_active_caja_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: caja/activa
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  caja_apertura:
    handler: src/handlers/caja/caja_manager.abrir_caja_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: caja/apertura
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  caja_movimiento:
    handler: src/handlers/caja/caja_manager.registrar_movimiento_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: caja/movimientos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  caja_cierre:
    handler: src/handlers/caja/caja_manager.cerrar_caja_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: caja/cierre
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  caja_arqueos:
    handler: src/handlers/caja/caja_manager.list_arqueos_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: caja/arqueos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  getConfig:
    handler: src.handlers.admin.configuracion_manager.get_config_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: configuracion
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  updateConfig:
    handler: src.handlers.admin.configuracion_manager.update_config_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: configuracion
          method: put
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  get_ventas:
    handler: src.handlers.ventas.ventas_manager.list_ventas_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ventas
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ventas_cxc:
    handler: src/handlers/ventas/ventas_manager.list_cxc_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ventas/cxc
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  ventas_abono:
    handler: src/handlers/ventas/ventas_manager.registrar_abono_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: ventas/{id}/pagos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  compras_create:
    handler: src/handlers/compras/compras_manager.create_compra_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: compras
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  compras_list:
    handler: src/handlers/compras/compras_manager.list_compras_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: compras
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  compras_get:
    handler: src/handlers/compras/compras_manager.get_compra_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: compras/{id}
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  compras_cancel:
    handler: src/handlers/compras/compras_manager.cancel_compra_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: compras/{id}/cancel
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  compras_pago:
    handler: src/handlers/compras/compras_manager.registrar_pago_proveedor_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: compras/{id}/pagos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  compras_cxp:
    handler: src/handlers/compras/compras_manager.list_cxp_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: compras/cxp
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_inventario_valuado:
    handler: src/handlers/contabilidad/contabilidad_manager.get_inventario_valuado_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/inventario-valuado
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_aging_cxc:
    handler: src/handlers/contabilidad/contabilidad_manager.get_aging_cxc_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/aging-cxc
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_aging_cxp:
    handler: src/handlers/contabilidad/contabilidad_manager.get_aging_cxp_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/aging-cxp
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_margen:
    handler: src/handlers/contabilidad/contabilidad_manager.get_margen_ventas_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/margen
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_resumen_mensual:
    handler: src/handlers/contabilidad/contabilidad_manager.get_resumen_mensual_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/resumen-mensual
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_iva_mensual:
    handler: src/handlers/contabilidad/contabilidad_manager.get_iva_mensual_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/iva-mensual
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_resumen_por_os:
    handler: src/handlers/contabilidad/contabilidad_manager.get_resumen_por_os_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/resumen-os
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_gastos_fijos_list:
    handler: src/handlers/contabilidad/contabilidad_manager.list_gastos_fijos_mes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/gastos-fijos
          method: get
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_gastos_fijos_upsert:
    handler: src/handlers/contabilidad/contabilidad_manager.upsert_gasto_fijo_mes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/gastos-fijos
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_gastos_fijos_seed:
    handler: src/handlers/contabilidad/contabilidad_manager.seed_gastos_fijos_mes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/gastos-fijos/seed
          method: post
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

  contabilidad_gastos_fijos_delete:
    handler: src/handlers/contabilidad/contabilidad_manager.delete_gasto_fijo_mes_handler
    layers:
      - !Ref PythonRequirementsLambdaLayer
    events:
      - http:
          path: contabilidad/gastos-fijos/{id}
          method: delete
          cors: true
          authorizer:
            name: cognitoAuthorizer
            type: COGNITO_USER_POOLS
            arn: !GetAtt CognitoUserPool.Arn

resources:
  Resources:
    S3BucketImages:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:service}-${self:provider.stage}-media
        PublicAccessBlockConfiguration:
          BlockPublicAcls: false
          BlockPublicPolicy: false
          IgnorePublicAcls: false
          RestrictPublicBuckets: false
        CorsConfiguration:
          CorsRules:
            - AllowedHeaders: ['*']
              AllowedMethods: [GET, PUT, POST, DELETE, HEAD]
              AllowedOrigins: ['*']

    S3BucketPolicy:
      Type: AWS::S3::BucketPolicy
      Properties:
        Bucket: !Ref S3BucketImages
        PolicyDocument:
          Statement:
            - Action:
                - s3:GetObject
              Effect: Allow
              Resource: !Sub "arn:aws:s3:::${self:service}-${self:provider.stage}-media/logotipos/*"
              Principal: "*"

    S3BucketEvidencia:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:service}-${self:provider.stage}-evidencia
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true
        CorsConfiguration:
          CorsRules:
            - AllowedHeaders: ['*']
              AllowedMethods: [GET, PUT, POST, DELETE, HEAD]
              AllowedOrigins: ['*']
              MaxAge: 3000

    # Cognito User Pool
    CognitoUserPool:
      Type: AWS::Cognito::UserPool
      Properties:
        UserPoolName: ${self:service}-${self:provider.stage}-user-pool
        AdminCreateUserConfig:
          InviteMessageTemplate:
            EmailSubject: "Bienvenido a SIGA - Credenciales de Acceso"
            EmailMessage: "Bienvenido al Sistema Integral de Gestión Automotriz, su usuario es:{username} y su contraseña temporal es: {####}"
        UsernameAttributes:
          - email
        AutoVerifiedAttributes:
          - email
        Policies:
          PasswordPolicy:
            MinimumLength: 8
            RequireLowercase: true
            RequireNumbers: true
            RequireSymbols: true
            RequireUppercase: true
        Schema:
          - Name: tenant_id
            AttributeDataType: String
            Mutable: true
            Required: false

    # Cognito User Pool Client
    CognitoUserPoolClient:
      Type: AWS::Cognito::UserPoolClient
      Properties:
        ClientName: ${self:service}-${self:provider.stage}-client
        UserPoolId: !Ref CognitoUserPool
        ExplicitAuthFlows:
          - ALLOW_USER_PASSWORD_AUTH
          - ALLOW_REFRESH_TOKEN_AUTH
          - ALLOW_USER_SRP_AUTH
        PreventUserExistenceErrors: ENABLED
        IdTokenValidity: 5
        AccessTokenValidity: 5
        RefreshTokenValidity: 30
        TokenValidityUnits:
          AccessToken: hours
          IdToken: hours
          RefreshToken: days

    # Cognito User Pool Groups
    CognitoGroupSuperAdmin:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: SUPER_ADMIN
        UserPoolId: !Ref CognitoUserPool
        Description: "Super Administradores del sistema"
        
    CognitoGroupAdmin:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: ADMIN
        UserPoolId: !Ref CognitoUserPool
        Description: "Administradores de los talleres"

    CognitoGroupAsesor:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: ASESOR
        UserPoolId: !Ref CognitoUserPool
        Description: "Asesores de los talleres"

    CognitoGroupMecanico:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: MECANICO
        UserPoolId: !Ref CognitoUserPool
        Description: "Mecánicos de los talleres"